/* eslint-disable prettier/prettier */
/* eslint-disable no-console */
/* eslint-disable prettier/prettier */
// const { Car } = require('../models');

const getAllSidebar = async (value) => {
    console.log(value);
    // const categories = await Car.find().distinct('typeCar');
    // return categories;
};

module.exports = { getAllSidebar };